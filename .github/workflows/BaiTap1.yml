name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Name
        run:  |
          export NAME="Nguyen Dang Hung"
          export H = date
          echo "$NAME" "date"
      
